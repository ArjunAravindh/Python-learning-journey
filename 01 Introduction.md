#01 Introduction
##What is Python
###Python is a high-level, interpreted programming language. This means that the code is processed at runtime by an interpreter, so there is no need to compie the program before executing it. It features a simple, conventional syntax that is incredibly close to plain English and standard algebraic notation. Furthermore, Python is highly interactive and inherently object-oriented, allowing developers to model real-world objects and build intuitive program structures.
